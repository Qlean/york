{
  "name": "york",
  "repository": {
    "type": "git",
    "url": "git://github.com/qlean/york.git"
  },
  "scripts": {
    "checkup": "lerna run build && lerna run test && lerna run lint",
    "release": "npm run checkup && lerna publish"
  },
  "devDependencies": {
    "lerna": "2.11.0"
  }
}
